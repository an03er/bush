name: CI/CD Pipeline

on:
  push:
    branches:
      - main

env:
  REGISTRY: ghcr.io
  # Важно: имя должно быть в нижнем регистре!
  IMAGE_NAME: ${{ github.repository }}

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      db:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: kubsu
          POSTGRES_PASSWORD: kubsu
          POSTGRES_DB: kubsu
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies and run tests
        run: |
          cd docker
          pip install ".[test]"
          export DATABASE_URL=postgresql+psycopg://kubsu:kubsu@localhost:5432/kubsu
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          pytest tests
        env:
          DATABASE_URL: postgresql+psycopg://kubsu:kubsu@localhost:5432/kubsu

  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    # Важно! Даем права на запись в пакеты
    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Аутентификация в GHCR с использованием GITHUB_TOKEN
      - name: Log in to GitHub Container Registry
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build Docker image
        run: |
          cd docker
          # Собираем с правильным тегом для GHCR
          docker build -t ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }} .

      - name: Push image to GHCR
        run: |
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}

      # Также помечаем как latest (опционально)
      - name: Tag and push as latest
        run: |
          docker tag ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }} ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

  deploy:
    needs: build-and-push
    runs-on: self-hosted
    permissions:
      contents: read
      packages: read  # Для self-hosted runner тоже нужны права на чтение
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Аутентификация на self-hosted runner
      - name: Log in to GitHub Container Registry
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
        # Если на сервере используется podman вместо docker, замените на:
        # echo "${{ secrets.GITHUB_TOKEN }}" | podman login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Deploy to Server
        run: |
          # Используем правильный полный тег образа из GHCR
          IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"
          
          echo "Pulling image: $IMAGE"
          # Замените docker на podman если нужно
          podman pull $IMAGE
          
          podman stop usersapp || true
          podman rm usersapp || true
          
          podman run -d --name usersapp --network host \
            -e DATABASE_URL="postgresql+psycopg://kubsu:kubsu@localhost:5432/kubsu" \
            -e ROOT_PATH="/${{ secrets.USERNAME }}" \
            $IMAGE

          podman ps
